# Exercise 5
# How do the set types differ from the sequence types?
# The set types are unordered and unique, and therefore not able to be indexed using whole numbers.
# The sequence types are ordered and not necessarily unique, and can be indexed using whole numbers.